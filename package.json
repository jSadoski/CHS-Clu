{
  "name": "chs-clu",
  "version": "0.1.0",
  "description": "Clu is a general-use bot tailored for CoolHandSkywalker's Community Discord server.",
  "main": "index.js",
  "scripts": {
    "debug": "concurrently \"node --inspect -r dotenv/config index.js\" \"npm run --prefix ./admin-client/ serve\"",
    "debug-client": "npm run --prefix ./admin-client/ serve",
    "build-client": "npm run --prefix ./admin-client/ build",
    "debug-api": "node --inspect -r dotenv/config index.js",
    "start": "concurrently \"node index.js\" \"npm run --prefix ./admin-client/ serve\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jSadoski/CHS-Clu.git"
  },
  "author": "Joe Sadoski <jdsadoski@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jSadoski/CHS-Clu/issues"
  },
  "homepage": "https://github.com/jSadoski/CHS-Clu#readme",
  "devDependencies": {
    "dotenv": "^8.2.0",
    "eslint": "^7.15.0",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "sqlite3": "^5.0.0"
  },
  "dependencies": {
    "concurrently": "^5.3.0",
    "cors": "^2.8.5",
    "discord.js": "^12.5.1",
    "express": "^4.17.1",
    "sequelize": "^6.3.5"
  }
}
